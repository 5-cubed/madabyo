# CMP-005 SidebarTree
- Status: Draft

## Responsibility
Stateless (props-driven) React component that renders the pruned markdown tree with VS Code icons, the light theme, and the empty/loading states.

## Mapped
- `web/src/components/SidebarTree.jsx` — new file

## Interfaces
- `<SidebarTree tree={TreeNode} status={'loading'|'ready'|'empty'|'unsupported'|'needs-permission'} onSelectFile={(fileHandle: FileSystemFileHandle) => void} onRegrantPermission={() => void} />`

## Depends On
- CMP-006 ThemeTokens

## Used By
- SEQ-003, SEQ-005, SEQ-006, SEQ-007, SEQ-009, SEQ-013, SEQ-024, SEQ-025, SEQ-026
