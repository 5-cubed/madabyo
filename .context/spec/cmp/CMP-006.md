# CMP-006 ThemeTokens
- Status: Draft

## Responsibility
Stateless module of light-theme color/spacing values (Token Light: editor bg `#faf9f5`, sidebar `#f6f5f1`, ink `#2a2920`, rust-orange accent `#9a4929`, per the token-light-theme-preview check) shared by every visual component.

## Mapped
- `web/src/theme/tokens.css` (or `tokens.js` if consumed as JS constants) — new file, supersedes `web/src/App.css` / `web/src/index.css` placeholder styling

## Interfaces
- CSS custom properties (e.g. `--color-editor-bg`, `--color-sidebar-bg`, `--color-ink`, `--color-accent`) consumed by any component's stylesheet

## Depends On
- (none — leaf module)

## Used By
- SEQ-003
