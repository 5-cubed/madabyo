# CMP-010 Pane
- Status: Draft

## Responsibility
Stateless (props-driven) React component rendering one pane's tab bar and active tab content, or an empty-state when it has no open tabs.

## Mapped
- `web/src/components/Pane.jsx` — new file

## Interfaces
- `<Pane tabs={TabEntry[]} activeTabId={string|null} onSelectTab={(fileId) => void} onCloseTab={(fileId) => void} />`

## Depends On
- CMP-008 TabManager (reads its exposed state via props)

## Used By
- SEQ-010, SEQ-011, SEQ-014, SEQ-015, SEQ-017
